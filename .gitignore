# ─────────────────────────────────────────────────────────────
#  Orbit — Android client (Orbit/) + Ktor server (OrbitKtorServer/)
#
#  Patterns without a slash match at ANY depth, so one rule here
#  covers both projects. Rule of thumb: if Gradle can regenerate
#  it, or it only makes sense on this machine, it stays out.
#
#  NOTE: comments must be on their own line. Git has no trailing
#  comments — "foo  # why" becomes a pattern containing a hash
#  and silently matches nothing.
# ─────────────────────────────────────────────────────────────

# ---- Build output ----------------------------------------------------------
# ~80 MB on the Android side alone. Recreated by ./gradlew build.
build/
captures/
out/
.externalNativeBuild/
.cxx/

# ---- Gradle ----------------------------------------------------------------
# local.properties holds YOUR Android SDK path — machine specific.
.gradle/
local.properties
gradle-app.setting

# ---- Kotlin ----------------------------------------------------------------
# Session directory from the Kotlin 2.x compiler daemon. Both projects make one.
.kotlin/

# ---- IDE -------------------------------------------------------------------
# Holds local JDK paths (gradleJvm) and per-machine window state.
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Per-machine Claude Code state: settings.local.json records permissions granted
# on THIS machine. A shared .claude/settings.json is fine to commit, so only the
# local overrides and scratch files are excluded.
.claude/settings.local.json
.claude/*.local.json

# ---- Server: embedded database files ---------------------------------------
# The server now runs on MySQL, which lives outside the repo entirely. These
# rules stay for the leftover H2 files from the scaffold: they are generated at
# runtime and rewritten on every run. The schema lives in code
# (db/DatabaseSchema.kt) and the sample data in db/seed.sql, both committed.
*.mv.db
*.trace.db
*.lock.db

# ---- Server: runtime logs --------------------------------------------------
logs/
*.log
hs_err_pid*

# ---- Signing & secrets -----------------------------------------------------
# A leaked keystore cannot be revoked — anyone holding it can publish updates
# that Android accepts as genuinely yours.
*.jks
*.keystore
keystore.properties
signing.properties
secrets.properties
*.env
.env
*.p12
*.pem

# ---- Build artifacts -------------------------------------------------------
# Note: *.jar is deliberately NOT ignored — gradle-wrapper.jar must be
# committed or ./gradlew fails for anyone who clones the repo.
*.apk
*.aab
*.ap_
*.aar
*.dex
*.class
output.json
release/

# ---- Lint / reports --------------------------------------------------------
lint-results*.html
lint-results*.xml
.navigation/

# ---- OS noise --------------------------------------------------------------
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# ---- Temporary files -------------------------------------------------------
# The last pattern is Word's lock file, created while a .docx is open.
*~
*.swp
*.tmp
*.bak
~$*.docx
